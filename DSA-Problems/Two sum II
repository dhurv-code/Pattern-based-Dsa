class Solution {
public:
    vector<int> twoSum(vector<int>& numbers, int target) {
        int n=numbers.size();
        int i=1, j=n-2;
        while(i<j){
            if(numbers[i]+numbers[j]==target)
                return{i,j};
        }
        i++;
        j--;
        return {};
    }
};
