void findLargestOddSubstring(string num) {
    // write your code here...
    int right=num.size()-1;
    for(right;right>=0;right--){
        int digits=num[right]-'0';
        if(digits%2!=0){
            cout<<num.substr(0,right+1);
            return;
        }
    }
    cout<<-1;
    
    
}
